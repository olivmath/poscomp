{
  "name": "poscomp-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "app:dev": "pnpm --prefix app dev",
    "app:build": "pnpm --prefix app build",
    "admin:dev": "pnpm --prefix admin dev",
    "admin:build": "pnpm --prefix admin build",
    "func:dev": "pnpm --prefix functions dev",
    "func:build": "pnpm --prefix functions build",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^13.31.0"
  }
}
